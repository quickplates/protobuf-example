version: v2

breaking:
  use:
    # Use breaking change detections for FILE category
    - FILE

  # Don't fail on breaking changes with unstable API versions
  ignore_unstable_packages: true

lint:
  use:
    # Use standard lint rules
    - STANDARD

modules:
  # Use module in src directory
  - path: src/
